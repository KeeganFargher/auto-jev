/**
 * Gathers the state a draft/upgrade decision needs to see — the compact
 * picture a Jev reasons over, not the whole game database.
 * TODO: shape isn't defined yet.
 */
export function buildState(): Record<string, unknown> {
  return {};
}
